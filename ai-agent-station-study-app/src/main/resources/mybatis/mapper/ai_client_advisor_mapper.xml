<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaoshuai66.ai.infrastructure.dao.IAiClientAdvisorDao">

    <resultMap id="AiClientAdvisorMap" type="com.xiaoshuai66.ai.infrastructure.dao.po.AiClientAdvisor">
        <id column="id" property="id"/>
        <result column="advisor_id" property="advisorId"/>
        <result column="advisor_name" property="advisorName"/>
        <result column="advisor_type" property="advisorType"/>
        <result column="order_num" property="orderNum"/>
        <result column="ext_param" property="extParam"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.xiaoshuai66.ai.infrastructure.dao.po.AiClientAdvisor" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_client_advisor (
            advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time
        ) VALUES (
            #{advisorId}, #{advisorName}, #{advisorType}, #{orderNum}, #{extParam}, #{status}, #{createTime}, #{updateTime}
        )
    </insert>

    <update id="updateById" parameterType="com.xiaoshuai66.ai.infrastructure.dao.po.AiClientAdvisor">
        UPDATE ai_client_advisor SET
            advisor_id = #{advisorId},
            advisor_name = #{advisorName},
            advisor_type = #{advisorType},
            order_num = #{orderNum},
            ext_param = #{extParam},
            status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM ai_client_advisor WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="java.lang.Long" resultMap="AiClientAdvisorMap">
        SELECT id, advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time
        FROM ai_client_advisor
        WHERE id = #{id}
    </select>

    <select id="selectByAdvisorId" parameterType="java.lang.String" resultMap="AiClientAdvisorMap">
        SELECT id, advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time
        FROM ai_client_advisor
        WHERE advisor_id = #{advisorId}
    </select>

    <select id="selectAll" resultMap="AiClientAdvisorMap">
        SELECT id, advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time
        FROM ai_client_advisor
    </select>
</mapper>